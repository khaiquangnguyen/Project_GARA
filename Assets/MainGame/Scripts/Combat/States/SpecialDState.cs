namespace GARA.Combat
{
    // One of four special-attack leaves (A/S/D/F) — behavior is entirely
    // inherited from DamagingSpineAnimationState; this class exists only so
    // it's its own distinct component type (see SpineAnimationState's
    // uniqueness note).
    public class SpecialDState : DamagingSpineAnimationState
    {
    }
}
