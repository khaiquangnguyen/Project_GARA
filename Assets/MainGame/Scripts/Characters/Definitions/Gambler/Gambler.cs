using GARA.Characters;

namespace GARA.Characters.Gambler
{
    // Gambler class prefab root. No cross-card validation needed today —
    // unlike Dancer, Gambler's cards aren't required to all be one input
    // type, so this stays an empty subclass.
    public class Gambler : CharacterDefinition
    {
    }
}
