using UnityEngine;

namespace GARA.Characters
{
    // On a [SerializeReference] field (or array): draws a dropdown of the
    // field type's concrete [Serializable] subclasses (SubclassPickerDrawer).
    public class SubclassPickerAttribute : PropertyAttribute
    {
    }
}
